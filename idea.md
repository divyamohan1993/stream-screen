Create a system which reads the screen and streams the screen live on another laptop which is listening to the ip on wifi network. it should auto create ports etc and not use any usable ports. the listener should auto listen to the traffic and show the screen live on another device. It is like the any desk software but it works on Wi-fi and it is free of cost and it's created by me so everything that any desk does it should do almost the same things but at free of cost and without any limits and on WI fi as the IPS are different to the assigned routers to the assigned service providers public IP is different private IP is different so we cannot route everything directly to the public IP for that we will use VPN later on but for now let's assume that this is how it works. Make it very fast Make it lowest memory taking assume live streaming as real time streaming Everything done should be in real time shown on another laptop
